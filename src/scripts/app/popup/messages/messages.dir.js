app.popup.messages.directive('messages', function () {
  return {
    restrict: 'E',
    controller: 'MessagesCtrl',
    link: function (scope, el, attrs) {
      scope.messages.view = {

      };


    }
  };
});
